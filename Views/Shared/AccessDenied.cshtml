@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <h1 class="display-1 text-danger"><i class="fas fa-ban"></i></h1>
            <h2 class="mb-4">Acceso Denegado</h2>
            <p class="lead">No tienes permisos para acceder a esta sección.</p>
            <a href="/" class="btn btn-primary mt-3"><i class="fas fa-home me-1"></i> Volver al inicio</a>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .display-1 {
            font-size: 7rem;
        }
    </style>
}
