@model TiendaApp.Models.Venta

@{
    ViewData["Title"] = "¡Compra Exitosa!";
}

<div class="container py-5 text-center">
    <div class="card border-0 shadow-sm mx-auto" style="max-width: 600px;">
        <div class="card-body p-5">
            <div class="mb-4">
                <div class="bg-success bg-opacity-10 d-inline-flex align-items-center justify-content-center rounded-circle" style="width: 100px; height: 100px;">
                    <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
                </div>
            </div>
            
            <h1 class="h3 mb-3">¡Gracias por tu compra!</h1>
            <p class="text-muted mb-4">Tu pedido ha sido procesado exitosamente.</p>
            
            <div class="card bg-light mb-4">
                <div class="card-body">
                    <h5 class="card-title mb-3">Detalles de la orden</h5>
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted">Número de orden:</span>
                        <strong>#@Model.Id</strong>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted">Fecha:</span>
                        <strong>@Model.FechaVenta.ToString("dd/MM/yyyy HH:mm")</strong>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                        <span class="text-muted">Total:</span>
                        <strong>@Model.Total.ToString("C")</strong>
                    </div>
                </div>
            </div>
            
            <p class="text-muted mb-4">
                Hemos enviado un correo electrónico con los detalles de tu compra. 
                Si tienes alguna pregunta, no dudes en <a asp-controller="Home" asp-action="Contact">contactarnos</a>.
            </p>
            
            <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                <a asp-controller="Home" asp-action="Index" class="btn btn-primary px-4">
                    <i class="fas fa-home me-2"></i>Volver al Inicio
                </a>
                <a href="#" class="btn btn-outline-secondary px-4">
                    <i class="fas fa-receipt me-2"></i>Ver Factura
                </a>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        body {
            background-color: #f8f9fa;
        }
    </style>
}
