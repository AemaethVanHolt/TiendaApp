@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card shadow">
                <div class="card-header bg-danger text-white">
                    <h2><i class="fas fa-exclamation-triangle me-2"></i>@ViewData["Title"]</h2>
                </div>
                <div class="card-body">
                    <div class="mb-4">
                        <i class="fas fa-lock fa-5x text-danger mb-3"></i>
                        <h3>No tienes permiso para acceder a este recurso.</h3>
                        <p class="lead">Parece que estás intentando acceder a una página a la que no tienes permiso.</p>
                    </div>
                    
                    <div class="mt-4">
                        <a href="javascript:history.back()" class="btn btn-outline-secondary me-2">
                            <i class="fas fa-arrow-left me-1"></i>Volver atrás
                        </a>
                        <a href="/" class="btn btn-primary">
                            <i class="fas fa-home me-1"></i>Ir a la página de inicio
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
